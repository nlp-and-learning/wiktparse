#pragma once
#include <string>
#include <vector>

class WikiData {
public:
    static void testFirstChunkWikiData();
    static void search(const std::vector<std::string> &listTosearch, const std::string & outputFIle);
};

